# ============================================
# GestionStore Backend — Variables locales
# ============================================
# Ce fichier est pour le développement LOCAL (sans Docker).
# Copiez-le en .env et modifiez les valeurs :
#   cp .env.example .env

# Base de données PostgreSQL
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/gestionstore?schema=public"

# Clés secrètes JWT (remplacez par des chaînes aléatoires longues en production)
JWT_SECRET="change-this-to-a-long-random-string"
JWT_REFRESH_SECRET="change-this-refresh-secret-too"

# Port du serveur
PORT=3001
