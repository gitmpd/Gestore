# ============================================
# GestionStore — Variables Docker Compose
# ============================================
# Ce fichier est utilisé par docker-compose.yml
# Copiez-le en .env et modifiez les valeurs :
#   cp .env.example .env

# Mot de passe de la base de données PostgreSQL
DB_PASSWORD=gestionstore_pwd

# Clés secrètes JWT (remplacez par des chaînes aléatoires longues en production)
JWT_SECRET=change-me-in-production-please
JWT_REFRESH_SECRET=change-me-refresh-too
